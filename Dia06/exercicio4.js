let tabuada = Number(prompt('Escolha a tabuada que deseja'))
    console.log('Primeira tabuada')

    for(let contador = 0; contador <= 3; contador++)
        console.log(tabuada + ' * ' + contador + ' = ' + tabuada * contador)

let tabuadaUm = Number(prompt('Escolha a tabuada que deseja'))
        console.log('Segunda tabuada')

    for(let contador = 0; contador <= 4; contador++)
        console.log(tabuadaUm + ' * ' + contador + ' = ' + tabuadaUm * contador)

let tabuadaDois = Number(prompt('Escolha a tabuada que deseja'))
        console.log('Terceira tabuada')

    for(let contador = 0; contador <= 5; contador++)
        console.log(tabuadaDois + ' * ' + contador + ' = ' + tabuadaDois * contador)